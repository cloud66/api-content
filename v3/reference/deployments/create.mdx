---
title: Redeploy Stack
scope: redeploy
path: /stacks/:stack_id/deployments
method: POST
response: |
  {
    "response": {
      "id": 108,
      "message": "Deployment scheduled",
      "queued": true
    }
  }
parameters:
  - name: stack_id
    presence: required
    data_type: string
    description: The stack UID
    sample_value: 5999b763474b0eafa5fafb64bff0ba80
  - name: git_ref
    presence: optional
    data_type: string
    description: Git reference (branch, tag or hash). Non-docker only.
    sample_value: a_git_tag_or_hash
  - name: services
    presence: optional
    data_type: string
    description: Deploy only the service(s) & image(s) specified (Docker only).
    sample_value: service1:image_tagA,service2:image_tagB
  - name: deployment_profile
    presence: optional
    data_type: string
    description: Deploy using a deployment profile
    sample_value: my-deploy-profile-name
  - name: user_reference
    presence: optional
    data_type: string
    description: Adds a value to the metadata field of the Stack Action (to allow you to match actions against your own identifiers). Max 255 chars.
    sample_value: useful-reference-123
---

Redeploy a stack.

<RelatedModels models={['Deployment']} />